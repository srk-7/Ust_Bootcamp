// create a birthday application capture user info, user dob by using typescript
// when we run the program we need to know who who users birthday is today
// if we select the month it should display the list of birthday in that month.
// if we type or select the year we need to get the birthdays in that specific year.

// write a typescript funtionality to read the excel sheets from the local machine
// the excel sheet consists of list of products XYZ mart
// now we need to develop  a billing application for every product there is id, name , price, quantity
// based on the items the bill should generate.