server:
  port: 8888
spring:
  application:
    name: DistrictWebClient
  datasource:
    url: jdbc:mysql://localhost:3306/ust
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true